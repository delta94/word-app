import React from "react";
import styled from "styled-components/native";

const View = styled.View``;
const Text = styled.Text``;

const WordTest = () => {
  return (
    <View>
      <Text>Word Test Screen!</Text>
    </View>
  );
};

export default WordTest;
